#To get the sequences and samples that are assigned to different haplogroups by PopART

#Remove all positions in fasta alignemnt file with "-" or "N"
python3 remove-all-nucleotie-in-aln-gap.py

#Count the number of seqs that are identical, output samples found with each sequence and sample count in haplogoup
awk '/^>/ {if (seq) {count[seq]++; headers[seq] = headers[seq] ", " hdr} hdr = $0; seq = ""} !/^>/ {seq = seq $0} END {if (seq) {count[seq]++; headers[seq] = headers[seq] ", " hdr} for (s in count) {sub(/^, /, "", headers[s]); print s, count[s], headers[s]}}' ska-align-k15-combined-out-p90-nofilter-justNs-no-gap-nucl.fasta > output-haps-counted.txt

awk '{ $1=""; $2=""; print $0 }' Falk-k15-weeded-m90-mapped-haps-counted.txt | sed 's/^[ \t]*//' | sed 's/>//g' | sed 's/ //g' > multisample-haplotypes-accessions.txt
#or#
awk '{ $1=""; $2=""; print $0 }' output-haps-counted.txt | sed 's/^[ \t]*//' | sed 's/>//g' | sed 's/ //g' > multisample-haplotypes-accessions.txt

##To rank haplotypes by distances

#Calculate the distance with "ska distance" and then average distance between samples to find the most to least divergent
ska distance Falk-k15-weeded-m90.skf > Falk-k15-weeded-m90-distances.txt

python3 calculate-average-sample-distance.py Falk-k15-m90-distances.txt | sed 's/\://g' | sed 's/ /\t/g' > Falk-k15-m90-distances-averages.txt

python3 filter_lesser_distance_samples_from_multihaplotypes.py multisample-haplotypes-accessions.txt Falk-k15-m90-distances-averages.txt Falk-k15-m90-distances-averages-non-multiple-sample-haps.txt